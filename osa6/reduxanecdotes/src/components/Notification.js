const Notification = () => {
  const style = {
    border: 'solid',
    padding: 10,
    borderWidth: 1
  };

  return (
    <div style={style}>
      The notification
    </div>
  );
};

export default Notification;